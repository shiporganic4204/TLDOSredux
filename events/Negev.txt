add_namespace = negev


country_event = {
	id = negev.1
	title = negev.1.t
	desc = negev.1.d
	picture = GFX_report_event_spain_civil_war_soldiers

	trigger = {
		tag = ISR
	}
	
	is_triggered_only = no
	
	fire_only_once = yes
	
	immediate = {
		hidden_effect = {
			set_variable = {
				var = shekels
				value = 10
			}
			set_variable = {
				var = spend
				value = 5
			}
			retire_country_leader = yes
		}
		
	}
	
}